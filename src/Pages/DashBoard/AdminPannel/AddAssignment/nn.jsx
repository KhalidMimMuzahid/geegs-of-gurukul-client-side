import React from "react";

const nn = () => {
  // const handleSearch = () => {
  //
  // };
  return (
    <div>
      {/* filterData.length > 0 &&
            filterData?.filter(admissions => {
                return search?.toLowerCase() === '' ? admissions : admissions.name.toLowerCase().includes(search?.toLowerCase()) ||
                 admissions.email?.toLowerCase().includes(search?.toLowerCase()) ||
                  admissions.phone.toLowerCase().includes(search?.toLowerCase())
            })?.map((admissions, i) =>
                
            ) */}

      {}
    </div>
  );
};

export default nn;
